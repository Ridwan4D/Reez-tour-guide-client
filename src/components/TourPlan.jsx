import PropType from 'prop-types'
const TourPlan = ({plan}) => {
    // console.log(plan);
    const {day,activities} = plan;
    return (
        <div>
            
        </div>
    );
};
TourPlan.propTypes ={
    plan: PropType.object
}
export default TourPlan;
import { Helmet } from "react-helmet";

const Login = () => {
    return (
        <div>
            <Helmet>
                <title>Login | Reez Tour Guide</title>
            </Helmet>
            <h1 className="text-4xl font-bold">This is Login page of tour guid for tourist</h1>
        </div>
    );
};

export default Login;